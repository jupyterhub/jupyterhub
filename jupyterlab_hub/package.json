{
  "private": true,
  "name": "jupyterlab_hub",
  "version": "0.1.0",
  "description": "JupyterLab extension intergating JupyterHub",
  "author": "Project Jupyter",
  "main": "lib/plugin.js",
  "keywords": [
    "jupyter",
    "jupyterlab"
  ],
  "scripts": {
    "build:src": "tsc --project src && node scripts/copyfiles.js",
    "build:extension": "node scripts/build_extension.js",
    "build": "npm run build:src && npm run build:extension",
    "clean": "rimraf lib && rimraf jupyterlab_hub/static",
    "prepublish": "npm run build"
  },
  "dependencies": {
    "jupyterlab": "^0.5.1",
    "url-join": "^1.1.0"
  },
  "devDependencies": {
    "fs-extra": "^0.30.0",
    "rimraf": "^2.5.4",
    "jupyterlab-extension-builder": "^0.6.2",
    "typescript": "^2.0.3"
  }
}
