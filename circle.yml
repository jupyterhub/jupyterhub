machine:
  services:
    - docker

dependencies:
  override:
    - ls

test:
  override:
    - docker build -t jupyterhub/jupyterhub .
    - docker build -t jupyterhub/jupyterhub-onbuild:${CIRCLE_TAG:-latest} -f Dockerfile.onbuild .

deployment:
  hub:
    branch: master
    commands:
      - docker login -u $DOCKER_USER -p $DOCKER_PASS
      - docker push jupyterhub/jupyterhub-onbuild:${CIRCLE_TAG:-latest}
