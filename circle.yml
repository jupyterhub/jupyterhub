machine:
  services:
    - docker

dependencies:
  override:
    - ls

test:
  override:
    - docker build -t jupyter/jupyterhub .
    - docker build -t jupyter/jupyterhub-onbuild -f Dockerfile.onbuild .
